import { PlanningEntrainement } from './planning-entrainement';

describe('PlanningEntrainement', () => {
  it('should create an instance', () => {
    expect(new PlanningEntrainement()).toBeTruthy();
  });
});
